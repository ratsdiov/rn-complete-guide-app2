// Example using global constants to define styles, see lesson 74.  Alternative approach in BodyText.js and TitleText.js

import { StyleSheet } from "react-native";

export default StyleSheet.create({
    bodyText: {
        fontFamily: 'open-sans',
    },
    title: {
        fontFamily: 'open-sans',
        fontSize: 18
    }
});